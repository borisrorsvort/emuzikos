- title t(:'.title')
- content_for :sub_nav do
  %ul#sub_sections
    %li
      = link_to t(:'buttons.back', :model => t(:'activerecord.models.testimonials')), testimonials_path(@user), :class => "button back"
  .clear
= render 'form'
