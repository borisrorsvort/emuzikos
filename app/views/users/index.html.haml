- title t(:'.title')

-if !@users.blank?
  %table
    %tbody
      -for user in @users
        %tr
          %td= user.username
          %td= user.instruments if user.instruments
    %thead
      %tr
        %th
          =  t(:'activerecord.attributes.user.username')
-else
  =t(:'.no_users')

